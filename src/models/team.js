export class Team{

    constructor(id, name, logo, winner, lega, timestamp ) {
        this.id = id;
        this.name = name;
        this.logo = logo;
        this.winner = winner;
        this.lega = lega;
        this.timestamp = timestamp;
    }
}

export default Team;